{
	"name": "worktree-scripts",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"lint": "biome check",
		"lint:fix": "biome check --write",
		"install-global": "cp src/new-worktree.ts ~/.local/bin/new-worktree && cp src/cleanup-worktree.ts ~/.local/bin/cleanup-worktree && chmod +x ~/.local/bin/new-worktree ~/.local/bin/cleanup-worktree"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"bun-types": "^1.3.6",
		"typescript": "^5.9.3"
	}
}
